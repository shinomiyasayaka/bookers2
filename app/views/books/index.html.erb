<h1>New book</h1>
<%= form_with model: @list do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
	
  <h4>Opinion</h4>
  <%= f.text_area :opinion %>
	
  <%= f.submit 'Create book' %>
<% end %>


<% @book.each do |book| %>
  <table>
    <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
    </tr>
    </thead>
    <tbody>
    <tr>
    <td><%= image_tag 'sample-author1.jpg' %></td>
    <td><%= book.title %></td>
    <td><%= book.opinion %></td>
    </tr>
    </tbody>
  </table>
<% end %>